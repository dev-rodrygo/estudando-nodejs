// for loop
const loop = [5, 6, 7, 8, 9];

//o for percorre toda a array loop
for (let i = 0; i < loop.length; i++) {
  const element = loop[i];

  console.log(`${element} x ${element} = ${element * element}`);
}
